# 排除 node_modules 目录
**/node_modules/

# 排除开发相关文件
.vscode/
.git/
.gitignore
makefile
todo.md

# 排除源码目录（只保留编译后的文件）
extension/src/
shared/src/
webview/src/

# 排除开发配置文件
extension/tsconfig.json
shared/tsconfig*.json
webview/tsconfig*.json
webview/vite.config.ts
webview/postcss.config.js
webview/tailwind.config.js

# 排除 package-lock.json 文件
**/package-lock.json

# 排除文档和示例
docs/
*.excalidraw

# 排除测试文件
**/*.test.*
**/*.spec.*

# 排除开发工具配置
.eslintrc*
.prettierrc*

# 排除构建工具文件
webview/dist/
out/

# 排除不必要的文件
.DS_Store
*.log

# 保留必要的文件
!extension/dist/
!shared/dist/
!webview/dist/
!package.json
!readme.md
!LICENSE